---
---

<section class="relative min-h-screen flex items-center justify-center pt-28 overflow-hidden">
  <div class="container mx-auto px-6 relative z-10">
    <div class="flex flex-col lg:flex-row items-center justify-between gap-12">
      <div class="flex-1 text-center lg:text-left animate-fade-in space-y-8">
        <div class="inline-block px-4 py-2 rounded-full bg-white/5 border border-white/10 backdrop-blur-sm mb-4 animate-slide-up">
          <span class="text-sm text-primary font-medium tracking-wide">🚀 Sistem & Network Uzmanı</span>
        </div>
        
        <h1 class="text-5xl md:text-7xl font-bold tracking-tight animate-slide-up" style="animation-delay: 100ms;">
          Merhaba, Ben <br />
          <span class="bg-clip-text text-transparent bg-gradient-to-r from-primary via-purple-500 to-secondary">
            Engin TÜRKER
          </span>
        </h1>
        
        <p class="text-lg md:text-xl text-gray-400 max-w-2xl lg:mx-0 mx-auto animate-slide-up" style="animation-delay: 200ms;">
          Albayrak Demir Çelik bünyesinde Bilgi İşlem Sorumlusu olarak görev yapmaktayım. 15 yılı aşkın süredir uluslararası projelerde sistem, network ve altyapı yönetimi konularında deneyim kazandım.
        </p>
        
        <div class="flex flex-col sm:flex-row items-center justify-center lg:justify-start gap-4 pt-4 animate-slide-up" style="animation-delay: 300ms;">
          <a href="#contact" class="px-8 py-4 rounded-full bg-gradient-to-r from-primary to-secondary text-white font-semibold hover:opacity-90 transition-opacity shadow-lg shadow-primary/25">
            İletişime Geç
          </a>
          <a href="#experience" class="px-8 py-4 rounded-full bg-white/5 border border-white/10 hover:bg-white/10 text-white font-semibold transition-colors backdrop-blur-sm">
            Deneyimlerimi Gör
          </a>
        </div>
      </div>

      <div class="flex-1 relative animate-fade-in" style="animation-delay: 200ms;">
        <div class="relative w-64 h-64 md:w-80 md:h-80 mx-auto">
          <!-- Animated Background Ring -->
          <div class="absolute inset-0 rounded-full bg-gradient-to-tr from-primary via-purple-500 to-secondary animate-spin-slow opacity-70 blur-2xl"></div>
          
          <!-- Image Container -->
          <div class="relative w-full h-full rounded-full border-2 border-white/10 overflow-hidden backdrop-blur-md bg-white/5">
            <img 
              src="/profile.png" 
              alt="Engin TÜRKER" 
              class="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-500 scale-105"
            />
          </div>

          <!-- Glass Floating Badge -->
          <div class="absolute -bottom-4 -right-4 px-6 py-3 rounded-2xl bg-white/10 border border-white/20 backdrop-blur-xl animate-bounce shadow-xl">
            <span class="text-xs font-bold uppercase tracking-wider text-white">15+ Yıl Tecrübe</span>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Decorative elements -->
  <div class="absolute bottom-0 w-full h-24 bg-gradient-to-t from-dark to-transparent"></div>
</section>

<style>
  .animate-spin-slow {
    animation: spin 8s linear infinite;
  }
  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
</style>
